# Leaf Shade Classifier — Python dependencies
# ============================================
# Pinned to the exact versions the model was validated against.
#
# CRITICAL: scikit-learn MUST stay at 1.7.2 — the three .pkl model files
# (MLP, scaler, label encoder) were saved with 1.7.2. Loading them under a
# different scikit-learn version may silently mis-predict or fail outright.
# Do NOT upgrade scikit-learn without retraining and re-saving all three files.
#
# Python: 3.11.x (see .python-version → 3.11.9)
# Verified working on 3.11.15 with the versions below.

# Versions matched to the desktop training pipeline (Blackheath) so the CR3
# decode + feature extraction reproduce the values the model was trained on.
# scikit-learn MUST stay 1.7.2 (model pickles). rawpy/opencv/numpy are matched
# because the RAW demosaic + colour stats must be identical to training.
scikit-learn==1.7.2
numpy==2.2.6
joblib==1.5.3
opencv-python-headless==4.13.0.90
rawpy==0.25.1
ExifRead==3.5.1
Flask==3.1.3
Werkzeug==3.1.8
