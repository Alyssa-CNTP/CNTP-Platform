'use client'

// Command Centre replaces the old admin-only KPI page.
// All role/permission filtering is handled inside CommandCentre itself.
export { default } from '@/components/dashboard/CommandCentre'
